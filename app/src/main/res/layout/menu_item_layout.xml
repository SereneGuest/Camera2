<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="wrap_content"
    android:minWidth="@dimen/menu_min_width"
    android:minHeight="@dimen/menu_item_height"
    android:layout_height="wrap_content"
    android:padding="@dimen/menu_layout_margin"
    android:gravity="center"
    android:background="?android:attr/selectableItemBackgroundBorderless">
    <ImageView
        android:id="@+id/item_icon"
        android:layout_width="@dimen/menu_img_size"
        android:layout_height="@dimen/menu_img_size"
        android:layout_marginTop="@dimen/menu_text_margin"
        android:scaleType="centerInside"
        android:background="@drawable/setting_icon_bg_normal"
        android:clickable="false"
        android:layout_gravity="center"/>
    <TextView
        android:id="@+id/item_text"
        android:clickable="false"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/menu_text_margin"
        android:textSize="@dimen/menu_text_size"
        android:textColor="@color/menu_text_color"
        android:layout_gravity="center"/>

</LinearLayout>